apiVersion: apps/v1
kind: Deployment
metadata:
  name: jenkins-cicd-demo
  labels:
    app: jenkins-cicd-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jenkins-cicd-demo
  template:
    metadata:
      labels:
        app: jenkins-cicd-demo
    spec:
      containers:
        - name: jenkins-cicd-demo
          image: jenkins-cicd-demo:latest
          ports:
            - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: jenkins-cicd-demo
spec:
  selector:
    app: jenkins-cicd-demo
  ports:
    - port: 80
      targetPort: 5000
